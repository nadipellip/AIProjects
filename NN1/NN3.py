import sys


def setGlobals(i):
    global INEQUALITY, RADIUS_SQUARE
    temp_str = i[0]
    for i in range(len(temp_str)):
        if temp_str[i].isDigit():


    print()


if sys.argv[1:]:
    setGlobals(sys.argv[1:])
